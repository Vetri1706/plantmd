import React from 'react';

// This component is deprecated.
// Authentication is now handled automatically via the environment variable process.env.API_KEY.
// This ensures security and prevents the need for manual key entry.

const ApiSetupView: React.FC<any> = () => {
  return null;
};

export default ApiSetupView;